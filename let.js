'use strict';


// let keyword avoid hoisting
// With let the variable can not be used before it has been declared.
let productId = 12;

console.log(productId);

// Will return 'undefined' instead of an error
console.log(productName);

var productName = 'Chain';
